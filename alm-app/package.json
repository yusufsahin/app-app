{
  "name": "alm-app",
  "private": true,
  "description": "ALM app – full lifecycle: build, test, deploy",
  "scripts": {
    "build": "npm run build:fe && npm run build:be",
    "build:fe": "cd frontend && npm ci && npm run build",
    "build:be": "cd backend && uv sync",
    "test": "npm run test:be && npm run test:fe",
    "test:be": "cd backend && uv run pytest tests -v --tb=short",
    "test:fe": "cd frontend && npm run test:unit",
    "test:e2e": "cd frontend && npx playwright test",
    "test:e2e:deployed": "cd frontend && cross-env PLAYWRIGHT_BASE_URL=http://localhost:3000 npx playwright test",
    "deploy": "docker compose up --build -d",
    "deploy:down": "docker compose down",
    "lifecycle": "npm run build && npm run test && npm run deploy",
    "lifecycle:full": "npm run build && npm run test && npm run deploy && npm run test:e2e:deployed"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
