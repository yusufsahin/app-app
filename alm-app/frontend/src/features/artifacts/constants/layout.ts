/**
 * Artifact detail panel sizing uses CSS variables in {@link ../../../styles/theme.css} (`:root` → `--artifact-detail-*`).
 * Split grid references `var(--artifact-detail-track)` in className; sheet uses the export below for `maxWidth`.
 */
export const ARTIFACT_DETAIL_SHEET_MAX_WIDTH_CSS = "var(--artifact-detail-sheet-max-width)";
