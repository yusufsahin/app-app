artifacts:
  - artifact_id: "manifest_sample.cycle.minimal"
    artifact_type: "manifest_sample"
    owner: "platform-team"
    criticality: "medium"
    version_policy: "semver"
    artifact_version: "1.0.0"

  - artifact_id: "schema.cycle.v1"
    artifact_type: "schema"
    owner: "platform-team"
    criticality: "critical"
    version_policy: "semver"
    artifact_version: "1.0.0"

  - artifact_id: "rule_pack.cycle.v1"
    artifact_type: "rule_pack"
    owner: "platform-team"
    criticality: "high"
    version_policy: "semver"
    artifact_version: "1.0.0"

  - artifact_id: "migration_spec.cycle.v1_to_v2"
    artifact_type: "migration_spec"
    owner: "platform-team"
    criticality: "high"
    version_policy: "semver"
    artifact_version: "1.0.0"

  - artifact_id: "schema.manifest.v1"
    artifact_type: "schema"
    owner: "platform-team"
    criticality: "high"
    version_policy: "semver"
    artifact_version: "1.0.0"

  - artifact_id: "rule_pack.manifest.v1"
    artifact_type: "rule_pack"
    owner: "platform-team"
    criticality: "medium"
    version_policy: "semver"
    artifact_version: "1.0.0"

  - artifact_id: "compatibility_baseline.alm.v1"
    artifact_type: "compatibility_baseline"
    owner: "platform-team"
    criticality: "high"
    version_policy: "semver"
    artifact_version: "1.0.0"
